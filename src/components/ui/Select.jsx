import React from 'react';
import { Select as DirectorSelect } from '../../components/director/SharedComponents.jsx';

export default function Select(props){
  return <DirectorSelect {...props} />;
}
